# DB-01: Alembic 설정.
# sqlalchemy.url 은 런타임에 core/db_migrations.py 가 DATABASE_URL 로부터
# 동기 드라이버 형태로 주입한다. 여기에 실제 DB 경로를 하드코딩하지 않는다.
[alembic]
script_location = migrations
prepend_sys_path = .
version_path_separator = os
