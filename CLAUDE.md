## 목적

이 문서는 `docs/re-plan.md`의 개선 계획을 안전하게 실행하기 위한 프로젝트 공통
지침이다. 이 프로젝트는 원래 codex 멀티에이전트로 구현하다 **클로드 코드 단일
에이전트 순차 실행**으로 전환했다(`docs/re-progress.md`의 PLAN-SIMPLIFY-02). P0
보안 문제부터 Phase 순서대로 수정하고, 계획에 없는 기능이나 리팩터링을 섞지 않는다.

이 문서는 공통 안전·실행 규칙만 유지한다. 과거 codex의 파일 잠금, 작업 패킷,
PM 위임, Wave 병렬 같은 조율 규칙은 사용하지 않는다. `git diff`가 곧 변경 기록이다.

## 우선순위와 기준 문서

지침은 다음 순서로 해석한다.

1. 사용자의 현재 요청과 명시적 승인
2. 이 `CLAUDE.md`
3. `docs/re-plan.md`
4. `docs/evaluation.md`
5. 기존 README와 기타 문서

- `docs/re-plan.md`: 작업 ID, Phase, 선행 조건과 완료 조건. 본문의 `Wave`·`소유
  파일`·`병렬 가능/금지` 표기는 멀티에이전트 시절의 잔재이며 참고 정보로만 읽는다.
- `docs/evaluation.md`: 문제 근거와 위험
- `docs/re-progress.md`: 실제 상태, 결정, 검증 결과
- `docs/contracts/`: 권한 행렬 등 공유 계약
- `docs/baseline.md`: Phase 0 기준선

계획과 코드가 다르면 먼저 차이를 조사한다. 현재 코드에 맞추려고 완료 조건을
조용히 낮추지 않는다.

## 요청 분류

### RE_PLAN_EXECUTION

다음은 계획 실행이다.

- 다음 Phase·작업 진행
- 특정 re-plan 작업 구현
- 이전 계획 작업 실패 수정·검증

`docs/re-plan.md`의 기본 결정과 완료 조건을 명세로 사용한다. 다음 경우만 구현 전
사용자 승인을 받는다.

- `DEC-01`~`DEC-08` 권장값과 다른 정책
- 선행 작업 또는 Gate 생략
- 실제 DB·사용자 데이터·Git 추적 상태의 파괴적 변경
- 이력 재작성, push, PR, 외부 배포
- 계획 밖 공개 API 호환성 파괴

### QUESTION

설명, 상태, 오류 원인과 기술 비교는 직접 조사해 답한다. 변경 요청이 없으면 코드와
외부 상태를 수정하지 않는다.

### OUT_OF_PLAN_CHANGE

계획 밖 기능·UI·데이터 모델·리팩터링은 현재 작업에 섞지 않는다. 위험을 확인하고
별도 범위와 완료 조건을 정의한다. 결과를 바꾸는 결정만 사용자에게 확인한다.

### PROJECT_STUDY

사용자가 `프로젝트 공부`라고 하면 `project-study` 서브에이전트의 프로토콜로
면접식 학습을 진행한다. 학습은 계획 실행과 동시에 하지 않고 프로덕션 코드를
수정하지 않는다. (면접은 사용자와 한 문항씩 주고받아야 하므로, 서브에이전트를
띄우는 대신 메인이 그 프로토콜을 직접 따른다.)

## 계획 실행 절차 (단일 에이전트 순차)

한 번에 하나의 Phase만 활성화한다.

1. `docs/re-plan.md`에서 작업·선행 관계·완료 조건을 확인한다.
2. `docs/evaluation.md`의 근거와 `docs/contracts/`의 계약을 확인한다.
3. `docs/re-progress.md`에서 결정, 완료 작업, 실패를 확인한다.
4. Git 상태에서 사용자 기존 변경과 이번 작업 범위를 구분한다.
5. 이전 Phase Gate와 선행 작업 완료를 확인한다.
6. 누락된 `DEC-01`~`DEC-08`은 계획의 권장값을 적용해 진행 기록에 남긴다.
7. 실제 코드와 focused 테스트를 읽어 필요한 변경을 좁힌다.
8. 변경 전 실패 테스트 작성 → 구현 → 해당 테스트 통과 순으로 직접 구현한다.

`docs/re-progress.md`가 없으면 `BASE-01`부터 시작한다. 후속 작업 요청도 선행 Phase를
암묵적으로 완료 처리하지 않는다.

## 상태 기록

`docs/re-progress.md`에 `Decisions`, `Task Status`, `Validation Log`,
`Known Limitations`을 유지한다. (`Active File Locks`와 `Task Packets`는 단일 순차
실행 전환으로 미사용이며 이력 보존용으로만 남아 있다.)

상태는 다음 값만 사용한다.

- `PENDING`: 선행 작업 미완료
- `READY`: 시작 가능
- `IN_PROGRESS`: 구현·검증 중
- `FAIL`: 코드·테스트 실패로 수정 필요
- `BLOCKED`: 승인 또는 외부 조건 필요
- `PASS`: focused 구현·검증 통과
- `DONE`: Phase 게이트(§3.2 표준 게이트)까지 통과하고 진행 기록에 반영 완료

`PASS`와 `DONE`을 구분한다.

기존 사용자 변경과 겹치면 수정을 멈추고 경로와 필요한 변경을 사용자에게 보고한다.
기존 변경을 복원·삭제·덮어쓰지 않는다.

## 서브에이전트 사용 방침

단일 순차 실행이 기본이다. 서브에이전트는 다음 경우에만 쓰고, 작은 기계적 편집은
위임하지 않고 인라인으로 직접 수행한다.

- **넓은 읽기 전용 탐색**: 문맥을 오염시키지 않고 여러 파일·명명 규약을 훑어야 할
  때 내장 `Explore` 에이전트를 사용한다.
- **보안 diff 적대적 리뷰**: 인증·비밀·셸 격리·배포 등 보안 경계를 바꾼 diff를
  Phase 게이트 전에 반박 검토할 때 `security-reviewer` 서브에이전트를 사용한다.
- **프로젝트 학습**: 면접식 학습 프로토콜은 `project-study`를 기준으로 한다.

## 검증 게이트 (§3.2 표준 게이트)

**게이트는 손으로 명령을 나열하지 말고 실행기로 1회 실행한다.**

```
bash scripts/gate.sh
```

작업 도중 중간 확인이 필요하면 `bash scripts/gate.sh --fast`를 쓴다(`npm ci`와 build
생략). `--fast`는 Phase 완료 게이트가 아니며 통과로 기록하지 않는다.

실행기가 묶어서 수행하는 검사는 다음과 같다.

```
python -m pytest backend/tests
npm --prefix frontend run lint
npm --prefix frontend test -- --run
npm --prefix frontend run build
docker compose config --quiet          # 운영·개발 두 프로필
python scripts/security_scan.py
공백 검사                               # 줄바꿈 무관, 추적 diff + 미추적 파일
```

실행기는 Compose 필수 환경변수를 안전한 합성값으로 채우고(값은 출력하지 않는다),
`npm ci`를 `package-lock.json` hash당 한 번만 실행하며, 추적 diff와 미추적 파일의
공백 위반을 나눠 보고한다. 실행기를 쓸 수 없는 상황에서만 위 명령을 직접 실행하고,
그 사실을 진행 기록에 남긴다.

보안 경계를 바꾸는 **Phase 3(인증·배포)과 Phase 4(셸 격리)** 는 위 게이트에 더해
`docs/re-plan.md`에 명시된 **실제 구동 검증**을 1회 추가한다. 실행기는 이것을
대신하지 않는다.

- 같은 명령은 게이트당 한 번만 실행한다. 전체 검증은 작업 경계에서만 한다.
- 전체 검색은 `.venv`, `venv`, `node_modules`, `dist`, build 산출물과 실제 DB를
  제외하고 대상 파일부터 좁힌다.
- 일반 `git diff --check`는 미추적 파일을 검사하지 않고, 이 저장소에는 index가
  CRLF인 파일이 섞여 있어 추가된 줄의 CR을 후행 공백으로 오검출한다. 실행기의
  공백 검사가 두 문제를 모두 처리하므로 `git diff --check`를 게이트 근거로 쓰지 않는다.
- 작업 범위 밖 기존 파일의 공백 위반은 `scripts/gate-whitespace-ignore.txt`에 사유와
  함께 경로만 등록하고 파일 자체는 수정하지 않는다. 목록은 짧게 유지한다.
- 실행하지 못한 필수 검사는 `NOT_RUN` 또는 `BLOCKED`로 기록한다.
- 테스트를 통과시키려고 보안 정책이나 인수 조건을 약화하지 않는다. 게이트 실패를
  ignore 목록으로 우회하지 않는다.

## Phase와 Gate

1. Phase 0부터 순서대로 실행한다.
2. 선행 작업을 건너뛰지 않는다.
3. Phase의 모든 작업이 `DONE`이면 Phase Gate를 한 번 실행한다.
4. Gate 실패는 원인 작업을 `FAIL`로 되돌려 수정한다.
5. Gate 통과 후 다음 Phase를 `READY`로 바꾼다.

`RELEASE-01`에서 즉석 수정하지 않는다. 실패 원인 작업으로 되돌아가 수정·검증 후
다시 실행한다.

## 데이터·보안·파괴적 변경

다음 값을 출력·문서·로그에 기록하지 않는다.

- JWT, WebSocket ticket, session ID
- 비밀번호, 해시, `SECRET_KEY`
- 사용자명, IP, 감사 로그
- SQLite 실제 레코드
- 인증서 개인 키와 환경변수 값

다음은 실행 직전 정확한 대상을 확인하고 사용자 승인을 받는다.

- 대량 Git 추적 제거
- 실제 SQLite DB 삭제·이동·덮어쓰기 또는 운영 migration
- Git 이력 재작성
- push, PR, 운영 배포
- 인증서·외부 서비스 변경

DB 작업은 복사본·임시 DB에서 backup → upgrade → rollback → 데이터 보존 순서로
검증한다. 실제 값을 fixture로 복사하지 않는다.

Git 작업은 사용자 변경을 되돌리지 않는다. commit·push·PR은 사용자가 명시적으로
요청한 경우만 수행한다. 대량 삭제와 기능 변경을 섞지 않는다.

## 기존 dirty worktree

dirty worktree를 정상 상황으로 본다.

1. 기존 변경과 이번 작업 파일을 구분한다.
2. 기존 삭제·미추적 파일을 복원·제거하지 않는다.
3. 대상 파일 diff를 읽고 기존 의미를 보존한다.
4. 의미 충돌이면 중지하고 사용자에게 보고한다.
5. 무관한 기존 실패를 이번 작업 실패와 구분한다.

## 완료 조건

작업은 다음을 모두 만족해야 `DONE`이다.

- 선행 작업과 결정 충족
- 작업 범위 밖 변경 없음
- 회귀 위험을 검증하는 테스트 존재
- 인수 조건과 focused 검사 통과
- 공유 계약과 구현 일치
- Phase 게이트(보안 Phase는 실제 구동 포함) 1회 통과
- 실행하지 못한 필수 검사 없음
- 결과와 제한사항을 `docs/re-progress.md`에 기록

Phase 완료는 모든 작업 `DONE`, Phase Gate 통과, P0/P1 이월 없음과 제한사항 기록을
요구한다. 전체 계획 완료는 `RELEASE-01`, `RELEASE-02`, P0/P1 해소, 목표 점수와 운영
제한사항 문서화를 요구한다.

## 사용자 보고

완료 작업·Phase, 변경 파일, 계약·동작 변화, 검사 결과, 미실행 검사, 남은 위험,
다음 `READY` 작업과 필요한 승인을 보고한다. 실행하지 않은 검사나 다음 Phase를
완료했다고 말하지 않는다.

## 공통 금지

- 선행 조건·Gate 생략
- 요청과 무관한 정리·리팩터링
- UI만으로 권한 보장 (권한은 반드시 서버에서 강제)
- 기본 비밀·관리자·테스트 인증 우회
- 실패 테스트 삭제·skip·완화
- broad exception으로 실패 은폐
- 실제 DB·개인정보를 테스트에 사용
- 실행하지 않은 명령을 실행했다고 보고
- 사용자 변경 덮어쓰기